---
const { sellers } = Astro.props;
---

<div class="relative overflow-x-auto shadow-md sm:rounded-lg">
  <table class="w-full text-sm text-left rtl:text-right text-gray-500 dark:text-gray-400">
    <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
      <tr>
        <th scope="col" class="px-6 py-3">Nombre</th>
        <th scope="col" class="px-6 py-3">Localización</th>
        <th scope="col" class="px-6 py-3">Acciones</th>
      </tr>
    </thead>
    <tbody>
      {
        sellers.map((seller) => (
          <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700 hover:bg-gray-50 dark:hover:bg-gray-600">
            <th scope="row" class="flex items-center px-6 py-4 text-gray-900 whitespace-nowrap dark:text-white">
              <img class="w-10 h-10 rounded-full" src={seller.avatar} alt={seller.name} />
              <div class="ps-3">
                <div class="text-base font-semibold">{seller.name}</div>
              </div>
            </th>
            <td class="px-6 py-4">{seller.location}</td>
            <td class="px-6 py-4">
              <a href={`/sellers/${seller.id}`} class="font-medium text-blue-600 dark:text-blue-500 hover:underline">
                ➡️
              </a>
            </td>
          </tr>
        ))
      }
    </tbody>
  </table>
</div>
